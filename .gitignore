# Godot 4+ specific ignores
.godot/
.import/
export.cfg
export_presets.cfg

# Godot-specific mono/C# ignores
.mono/
data_*/

# Build output
build/

# System / editor files
.DS_Store
Thumbs.db

# Claude Code local settings
.claude/settings.local.json
data/.secret.env

# Gradle (plugin Android) — cache locale e marcatore del template di build Godot.
# `build/` piu' sopra copre gia' android/build/ e revenuecat_plugin/build/.
.gradle/
android/.build_version

# keystore passwords for Android export
export_presets.cfg.credentials

# official keystore files for Android export
*.jks

*.secret